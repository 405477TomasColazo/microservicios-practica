server:
  port: 8083
spring:
  application:
    name: moto-service
  doc:
    swagger-ui:
      path: /documentacion
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
    instance:
      hostname: localhost