server:
  port: 8081
spring:
  application:
    name: usuario-service
  doc:
    swagger-ui:
      path: /documentacion
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
    instance:
      hostname: localhost